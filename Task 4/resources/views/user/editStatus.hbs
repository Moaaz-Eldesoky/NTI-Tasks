{{>header}}
{{>navbar}}
<section class="container my-5">
    <form method="post">
        <div class="mb-3">
            <input placeholder="status" name="status" class="form-control" value="{{statusData.status}}">
        </div>
        <div class="mb-3">
            <button class="btn btn-primary">Update</button>
        </div>
    </form>
</section>
{{>footer}}